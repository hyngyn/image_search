<h1><%=params[:search]%></h1>

<h2>photo id</h2>
<% @photos.each do |photo| %>

<%= image_tag 'http://farm' + photo.farm.to_s + '.static.flickr.com/' + photo.server.to_s + '/' + photo.id.to_s + '_' + photo.secret + '.jpg' %>

<% photo.id %>
<% end %>


